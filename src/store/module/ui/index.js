import reducer, * as actionTypes from 'store/module/ui/reducer';
import * as actions from 'store/module/ui/action';

export const UiActionTypes = {
  ...actionTypes,
};

export const UiActions = {
  ...actions,
};

export default reducer;
